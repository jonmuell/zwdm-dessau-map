a.list-group-item.list-group-item-action
    .d-flex.w-100.justify-content-between
        h5.mb-1 {{ event.title }}
        small(v-if="this.isToday") HEUTE
    p.mb-1 {{ event.time }}
    small {{ event.location }}